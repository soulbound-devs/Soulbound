ext {
    //-- DEPENDENCIES
    forgeVersion = "1.20.1-47.0.3"
    mixinVersion = "0.8.5"
    reflectVersion = "0.10.2"
    terrablenderVersion = "1.20.1-3.0.0.164"
    geckolibVersion = "4.2"

    supportDependencies = [
            forge: "net.minecraftforge:forge:${forgeVersion}",
            processor: "org.spongepowered:mixin:${mixinVersion}:processor",
            terrablender: "com.github.glitchfiend:TerraBlender-forge:${terrablenderVersion}",
            reflect: "org.reflections:reflections:${reflectVersion}",
            geckolib: "software.bernie.geckolib:geckolib-forge-1.20:${geckolibVersion}"
    ]
}
